# 项目约束

## Web 工作现场

- 工作模式与管理模式切换时，协调助手视图必须保持挂载；不得因导航重建 Pi 会话、终止进行中的 Turn，或丢失草稿、阅读位置和焦点。
- 隐藏工作面期间完成的异步加载仍需保留恢复与滚动补偿状态，待工作面重新可见后再执行布局测量。

## 管理模式

- 管理页面通过应用 Shell 的稳定页面容器挂载；新增页面应复用现有模式切换和返回接口。
- 尚未实现的管理能力不得显示为可操作入口，也不得使用模拟业务数据表示成功或可用状态。

## 模型配置与恢复

- 模型目录、能力与认证由 Pi SDK 提供；配置 DTO、SQLite、模型元数据和公开事件不得保存或回显凭据。
- 已设置的默认模型失效时保留引用并阻止替代模型初始化；只有明确未设置默认时才使用基础配置。
- 全局默认只用于真正新建的会话；已绑定或待对账恢复的 Pi 会话不得套用当前新默认。
- 基础 Pi 配置与受控模型配置必须显式区分来源，恢复不得改变认证、协议或端点语义。
- Pi session 与 SQLite binding 无共同事务；模型初始化意图须先于 session 发布持久化，恢复必须按 session 身份核对无凭据选择记录。
